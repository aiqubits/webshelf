apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: webshelf
  labels:
    app: postgres
spec:
  serviceName: postgres-service
  replicas: 1
  podManagementPolicy: Parallel
  # OnDelete is intentional for a single-replica database StatefulSet:
  # it prevents automatic Pod restarts on spec changes (e.g., resource
  # limits adjustments), giving operators full control over when the
  # database restarts. For multi-replica setups, use RollingUpdate.
  updateStrategy:
    type: OnDelete
  # NOTE: Uses a manually-managed PVC instead of volumeClaimTemplates.
  # For a single-replica database, manual PVC management is simpler and
  # sufficient. If you scale beyond 1 replica, switch to volumeClaimTemplates.
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        fsGroup: 999
      containers:
      - name: postgres
        image: postgres:16
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: postgres
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: webshelf-secrets
              key: postgres_password
        - name: POSTGRES_DB
          value: webshelf
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: webshelf
spec:
  selector:
    app: postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: webshelf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
